# DECISIONS.md

# ARSM Suite 关键决策记录

> 本文件记录当前阶段仍有效的架构、范围和交付决策。历史细节见 Git 与归档 WORKLOG。

## D-20260723-01：当前主线继续使用现有数据库和架构

### 决策

```text
history.db / SQLite 继续作为唯一业务真源
LibraryVault 继续作为唯一正式数据库访问入口
当前 main 继续作为唯一产品主线
```

### 原因

- 已承载真实历史和 100+ 混合状态任务；
- 下载、迁移、资源库和 Tools 已围绕该模型完成测试；
- 替换数据库会产生旧任务、旧 `.part` 和路径兼容风险。

### 禁止

- 新建第二套正式 `library.db`；
- 使用 v2 `download_tasks/download_files` 替换现有表；
- UI 自行连接 SQLite。

---

## D-20260723-02：放弃 ARSM Library v2 独立分支

### 决策

`ARSM-Library-v2-source-20260722` 不继续开发、不合并、不作为当前版本替代。

### 原因

- 不继承旧数据库、队列和 `.part`；
- 正式库写入边界矛盾；
- 续传和 416 处理弱于当前主线；
- 重复目录检测存在缺陷；
- 真实 API 认证和 Windows 证据缺失。

### 允许

只吸收设计思想，并在当前架构中重新实现和测试。

---

## D-20260723-03：选择性吸收 v2 优点

### 纳入后续

- Service/read model；
- 批量队列快照；
- metadata 与 audio 并发池分离；
- 批量添加预览；
- 显式状态迁移；
- 页面 active/inactive 生命周期；
- 资源库详情侧栏；
- 后续可选托盘模式。

### 不纳入

- 新数据库和新下载表；
- v2 下载引擎；
- v2 设置持久化；
- 正式库直入开关；
- 整套 v2 UI/托盘源码。

---

## D-20260723-04：T10 只做低风险内部优化

### 决策

`TAKEOVER-T10` 聚焦 Queue Service 和大队列性能。

### 范围

- 下载只读模型；
- LibraryVault 批量快照；
- Service 门面；
- 批量 RJ 预览；
- metadata queue；
- 页面生命周期；
- 状态迁移规则。

### 边界

```text
不改数据库 schema
不替换下载核心
不改 .part 语义
不访问正式数据
不加入托盘
不开发播放器
```

---

## D-20260723-05：机器验收与视觉验收分离

### 决策

Windows 验收必须将机器事实与视觉判断分开。

### 机器事实

- 启动、PID、关闭、日志、文件位置、哈希和网络状态。

### 视觉判断

- 页面切换、黑屏、乱码、重叠、截断和布局。

### 规则

- DeepSeek 不负责视觉 PASS；
- `Kill()` 不证明正常关闭；
- 401 不证明下载成功；
- 没有页面截图不得判定视觉通过。

---

## D-20260723-06：正式维护操作继续冻结

### 决策

在现有混合任务清空或进入维护窗口前，继续冻结：

- External Intake execute；
- 正式迁移、移动、隔离和删除；
- VACUUM；
- backlog execute；
- T7 正式小批量目录整理。

普通产品优化不得顺带开放这些入口。

---

## D-20260723-07：播放器继续延后

### 决策

播放器不进入 T10~T14 的当前优先队列。

### 开始条件

- 大队列性能稳定；
- Desktop 视觉证据完成；
- 真实下载路径明确；
- 没有高优先级数据库/下载缺陷。

---

## D-20260723-08：工作日志分段归档

### 决策

旧 `WORKLOG.md` 原样归档到：

```text
docs/archive/WORKLOG_20260627_20260721.md
```

根目录 `WORKLOG.md` 从 Post-RC 阶段重新开始。

### 原因

- 旧日志约 2700 行，顶部当前状态严重过时；
- 保留完整历史的同时提高当前文档可读性；
- Git 历史和归档文件均可追溯。
